# lakejobai-job-radar · 项目能力盘点与实现情况

> 盘点时间：2026-09-10 ｜ 口径：通读全部文档（README / SKILL / SCRIPT / CHANGELOG / CHANGES / CONTRIBUTING / filter_rules_draft / docs/*）+ 交叉核对核心代码（boss_app / boss_state / boss_firefox / boss_automation / boss_replier / boss_company / match_report / lakejob_cli / interview）
>
> 状态标记：**✅ 已实现** · **⚠️ 部分/未对接** · **🔲 规划未落地**

---

## 一、一句话结论

这是一个**以 BOSS 直聘自动化为核心的个人求职工具**，主体功能（搜索、翻页投递、公司去重、HR 活跃度过滤、AI 三大智能体、简历-JD 匹配报告、CLI、安全加固）**已落地且代码与文档基本一致**；但存在**三处明确缺口**：① 文档极力宣传的「岗位画像聚类」(cluster.py) **仅停留在方案文档，未写代码**；② CLI 的 `smart-send`/`company-preview` **命令与 schema 条目存在，但后端路由从未挂载，是死链**；③ v4.1 已知「简历优化/沟通建议前端弹窗未对接」。文档与代码在 CLI 命令数、API 清单、依赖声明上有明显漂移。

---

## 二、能力总览表

| 模块 | 能力 | 状态 | 证据 |
|---|---|---|---|
| boss_app.py | Web 后端 / REST+WS / 51 个路由 | ✅ | grep 路由 51 条 |
| boss_state.py | SQLite 数据层（投递/聊天/设置/公司缓存/候选池） | ✅ | 全量 schema + CRUD |
| boss_firefox.py | BOSS 爬虫（搜索/详情/福利/HR活跃/滚动） | ✅ | `BossScraper` 全方法 |
| boss_automation.py | 自动化投递 + 聊天监控 + AI 接管 + 交换微信/简历 | ✅ | 含 `run_chat_monitor_cycle`(1569) 接 `generate_reply` |
| boss_replier.py | 招呼语 / 智能招呼语 / AI 回复 | ✅ | `generate_greeting`/`generate_smart_greeting`/`generate_reply` |
| match_report.py | 简历-JD 匹配报告（采集+排序+关键词缺口+方向过滤+软件向） | ✅ | 已有 4 份 09-10 报告 |
| lakejob_cli | 命令行（Agent 友好 JSON 信封） | ⚠️ | 实际 20 命令，文档写 18/16/14 不一致 |
| interview/ | 面试刷题子服务（出题/批改/embedding 检索/弱项） | ✅ | 独立 FastAPI，需 Ollama |
| boss_company.py | 公司画像聚合（供 smart-send） | 🔲 | 仅辅助函数，路由未挂载 |
| 画像聚类 cluster.py | 岗位画像聚类（embedding+LLM 命名） | 🔲 | 文件不存在，仅 docs 方案 |

---

## 三、分模块详述

### 3.1 Web 控制台后端 `boss_app.py`
- **FastAPI**，单例 `automation`（Playwright 单线程池 `_playwright_executor`），WebSocket 实时推送（`/ws`）。
- **已挂载路由（51 个）**：岗位搜索/列表/投递/批量/扫描/扫描投递/AI 分析/简历优化/沟通建议；会话列表/消息/同步/发送/暂停/恢复/打开；候选池增删查；微信交换记录；系统启停/重登/心跳/监控暂停恢复/跳转聊天；公司信息/缓存/查重/已投公司；设置读写；districts；debug 选择器；health。
- **安全加固（v4.2 已落地）**：CORS 收紧为 `^https?://(127\.0\.0\.1|localhost|\[::1\])(:\d+)?$`（L81）；`GET /api/settings` 不回显明文 `ai_api_key`（L2289 置空，靠 `ai_key_configured` 标志）。
- 内联了巨大的 `CITY_MAP`（60+ 城市分组）+ `DISTRICTS`（区县 code），所以 `boss_geo.py` 实际**未被引用**（CHANGELOG 已注明）。

### 3.2 数据层 `boss_state.py`
- 表：`applications` / `conversations` / `messages` / `settings` / `daily_stats` / `shortlists` / `companies`（公司信息缓存 24h TTL）。
- 关键能力全实现：公司去重 `_normalize_company_name` + `has_company_been_applied`（精确+归一化模糊，排除"字节外包"误伤）、HR 活跃度列、`search_kw` 字段（报告隔离依据）、AI 24h 缓存列（`optimize_result`/`chat_suggestion_result`）、候选池、岗位列表上限 2000 条滚动淘汰。
- 迁移方式：启动时 `init_db()` 内 `CREATE TABLE IF NOT EXISTS` + 一堆 `ALTER TABLE`（兼容旧库），符合 CONTRIBUTING 约定。

### 3.3 爬虫 `boss_firefox.py`
- `BossScraper`：登录、`search`（薪资/经验/学历/规模/融资/福利/区 code 透传）、`_scroll_all`（持续滚动补全）、`_extract_job_cards`（抓 companyId/brandName/hrActive）、`fetch_detail`（JD 全文 + HR 信息）、`parse_hr_active`（"3日内活跃"等解析）、`_filter_by_welfare`。
- 风控：`page.evaluate(fetch)` 浏览器内调用自动带 cookie；`check_page_safety` 检测异常页。

### 3.4 自动化 `boss_automation.py`
- 投递：`apply_to_job`（带每日上限 429 拦截、去重、HR 过滤）、`apply_batch`、`go_to_next_page`、`scan_and_apply_all_pages`（多页扫描默认 5 页，1–20 封顶）、`scan_current_page`。
- 聊天 AI 接管：`run_chat_monitor_cycle`（L1569）→ 检测未回复 HR 消息 → `boss_replier.generate_reply`（L1894/1918）→ 自动发送，**遵守 `auto_reply_enabled` 开关 + 每日自动回复上限**。后台循环 `chat_monitor_loop`（boss_app L2510）驱动。
- 交换通道：`send_wechat` / `send_phone` / `send_resume`（L1318/1390/1460，innerText 精确匹配兜底）。

### 3.5 AI 回复/招呼语 `boss_replier.py`
- `generate_greeting`（固定模板，`{job_title}` 占位）、`generate_smart_greeting`（LLM 读 JD 生成 3 痛点+效果付费话术，失败回退模板）、`generate_reply`（结合简历摘要+JD+历史消息+沟通风格生成回复）、`build_reply_context`。
- 智能招呼语联动简历优化结果：boss_app `analyze`/`optimize-resume` 会把简历摘要注入 prompt。

### 3.6 简历-JD 匹配报告 `match_report.py` ✅（重点项目，最完整）
- **采集**：`collect()` 复用 `BossScraper`，逐组合翻页抓 JD 全文入库；主动检测 Web 控制台浏览器冲突（`_check_web_console_conflict`，L290）。
- **隔离与过滤**：`search_kw` 写入→`WHERE search_kw IN (...)` 隔离；`_expand_search_kws`（L530）实现**宽词⊃窄词方向性包含**（项目经理→{项目经理,AI项目经理}，反向不吞）；`_split_jobs`（L724）+ `KW_FILTER_RULES`（L668，按方向 require/exclude/keep 规则）+ `_is_tech_job`（软件向默认开启）；弱相关/方向不符/非软件/噪音全部完整列清单。
- **报告**：`analyze_market`（市场热词）/ `analyze_match`（逐岗 LLM 打分，未配 Key 降级关键词覆盖率，缓存 `match_cache.json`）；章节含市场画像/技能 TOP30/分类视图/岗位样本/各剔除清单/关键词缺口（🔴≥10/🟡≥5/🟢）。
- **已产出 4 份报告**（reports/，2026-09-10，武汉，FDE/AI测试/项目经理/AI项目经理，·软件向），覆盖写。

### 3.7 CLI `lakejob_cli`
- **实际定义 20 个命令**（cli.py）：`version, schema, search, status, stats, jobs, apply, apply-batch, scan, scan-apply, conversations, chat, send, doctor, login, analyze, shortlist, server, restart, smart-send`。
- 输出：`output.py` 统一 JSON 信封（ok/command/data/error/pagination），stdout 强切 UTF-8 防 GBK 乱码；`client.py` 是 httpx 客户端。
- **问题**：`restart` + `smart-send` 不在 README/SKILL 命令表；`schema.json` 只列 14 条且含未实现的 `company-preview`。

### 3.8 面试刷题子服务 `interview/`
- **独立 FastAPI 服务**（interview/main.py，自带头 `app`，`interview/start.sh` 启动），与主控台解耦。
- 能力：`InterviewEngine` + 出题/作答/批改（`start_interview`/`next_question`/`submit_answer`）、知识库学习（`learn_ask`/`add_qa`/`search_qa`）、会话管理、弱项分析（`weak_areas`）、embedding 刷新（`refresh_embeddings`）。
- **依赖**：共享 `llm_client.py`——`get_embedding`（Ollama `nomic-embed-text` 768 维）、`llm_chat_ollama`（本地 `qwen2.5:14b`）、`llm_chat_deepseek`（API Key）。**出题需本地 Ollama；批改需 DeepSeek Key**（usage-guide 已注明）。
- 同时被 `match_report.py` 与 boss_app 复用（靠 `sys.path.insert("interview")` 引入）。

### 3.9 公司画像 `boss_company.py` 🔲
- 仅有 `build_company_preview` / `rank_companies_by_position_count` / `find_top_company_in_db` 等**辅助拼装函数**（调 boss_automation 的 `goto_company_similar_jobs`/`aggregate_company_hrs`）。
- **没有对应路由挂载到 boss_app**：`/api/companies/preview`、`/api/companies/smart-send` 在 boss_app.py 路由表中**不存在**，仅有单公司模式的 `/api/company/info`、`/api/company/cache/{name}`、`/api/company/check-applied`、`/api/companies/applied`。

---

## 四、关键缺口与风险（文档宣称 vs 代码事实）

### 🔴 缺口 1：smart-send / company-preview 是死链
- **事实**：`lakejob_cli/cli.py` L323 定义 `smart-send`，`client.py` L132/L158 调 `GET /api/companies/preview`、`POST /api/companies/smart-send`；`schema.json` L113/L126 也把它们列为 Agent 工具。
- **但** boss_app.py 路由表无这两条；`boss_company.py` 只写了拼装函数 + `tests/test_smart_send.py` 有单测。**运行 `lakejob smart-send` 会 404/500。**
- **影响**：跨公司聚合挑最高职级 HR 再投递这条高级路径不可用；schema 给 Agent 的"智能投递"工具是假的。

### 🔴 缺口 2：岗位画像聚类（cluster.py）完全未实现
- `docs/llm-analysis-upgrade-plan.md` 是 2026-09-10 的 v1 头脑风暴方案（三阶段：画像聚类→匹配度多维核算→求职策略层），设计详尽（A+B 混合、双层缓存、降级全覆盖）。
- **代码层面 cluster.py 不存在**，`boss_state.py` 无 `job_embeddings`/`report_clusters` 表，**零落地**。属规划资产，不是已实现功能。

### 🟡 缺口 3：简历优化/沟通建议 Web 弹窗未对接（v4.1 已知）
- 后端 3 个 AI 端点（analyze/optimize-resume/chat-suggestion）齐全且带 24h 缓存，但 README 明确"dashboard.html 重写后弹窗 UI 暂未对接"。即 Web 端这两按钮可能点不开或走降级。

### 🟡 缺口 4：boss_geo.py 冗余
- 城市/区映射已内联进 boss_app.py（巨型 CITY_MAP/DISTRICTS），`boss_geo.py` 保留但**未被搜索流程引用**（CHANGELOG 注明）。可清理或补接。

### 🟡 缺口 5：文档/代码漂移
| 项 | 文档说 | 代码实际 |
|---|---|---|
| CLI 命令数 | README 18 / SKILL 16 / schema 14 | **20**（多 restart、smart-send） |
| API 文档 | README 列 ~30 条 | 实际 51 条，缺 company/info·cache·check-applied·companies/applied·districts·debug/*·conversations/{id}/open·applications/clear·conversations/clear；多 schema 的 company-preview/smart-send（未实现） |
| 依赖 requirements.txt | 未列 numpy/psutil/scikit-learn | 代码 import numpy（llm_client）、psutil（cli 杀进程兜底）、scikit-learn（规划需装） |
| 版本号 | README "v4.1"/"v4.2" 混用，pyproject 0.1.0 | 后端 `app.version="1.0.0"`，CLI `0.1.0` |

---

## 五、文档资产清单（已读）

| 文件 | 性质 | 关键内容 |
|---|---|---|
| README.md | 主文档 | 能力地图、18 命令表、API 参考、架构、隐私、已知限制 |
| SKILL.md | Agent 集成指南 | Core Loop、18 命令参考、输出契约、安全合规 |
| SCRIPT.md | 口播文案 | 产品介绍脚本（对外宣传用） |
| CHANGELOG.md | 变更日志 | v4.2 安全加固+match_report；v4.1 PR#3 合并+3 AI 端点；v4.0 全功能演进 |
| CHANGES.md | 优化说明 | 公司去重/翻页/公司画像/HR 活跃/UI/CLI/迁移 的具体代码改动 |
| CONTRIBUTING.md | 贡献约定 | 扁平布局、CLI 在 lakejob_cli、手动 ALTER 迁移、单文件前端 |
| filter_rules_draft.md | 过滤规则 | 弱相关方向过滤终版（4 方向 require/exclude/keep）+ 落地效果 |
| docs/usage-guide.md | 新手指南 | 场景一(报告)/二(投递)/三(Agent)，AI Key 影响矩阵 |
| docs/llm-analysis-upgrade-plan.md | **规划方案** | 画像聚类三阶段设计（🔲 未实现） |
| reports/*.md | 产出物 | 4 份 09-10 武汉匹配报告（验证 match_report 跑通） |
| scripts/refine_search_kw.py | 一次性脚本 | 按标题重写 search_kw 修正归属错位 |

---

## 六、给峰爷的优先级建议

1. **P0（修死链）**：把 `boss_company.build_company_preview` + `aggregate_company_hrs` 接成 `/api/companies/preview`、`/api/companies/smart-send` 两个路由挂进 boss_app，让 `smart-send` 真正可用；或反之从 CLI/schema 移除这两个条目，避免 Agent 误调。
2. **P1（补文档）**：统一 CLI 命令数（实际 20）与 API 清单到 README/schema；补 requirements（numpy/psutil），规划项标 scikit-learn。
3. **P1（接前端）**：把 v4.1 遗留的简历优化/沟通建议弹窗接回 dashboard.html（后端已就绪）。
4. **P2（规划）**：若要做画像聚类，按 `docs/llm-analysis-upgrade-plan.md` 落地 cluster.py（注意该方案要求"第一阶段纯 JD、不引入候选人字段"的红线）。
5. **P3（清理）**：评估 boss_geo.py 取舍；统一版本号语义。

---

## 七、架构一图（C4 语境级）

```
[Web 控制台 dashboard.html] ──HTTP/WS──┐
[CLI lakejob (JSON 信封)] ──HTTP────────├──► [boss_app.py FastAPI]
                                        │        │
                                        │        ├─ boss_automation (Playwright/Firefox)
                                        │        ├─ boss_replier (AI 招呼语/回复)
                                        │        ├─ boss_company (画像, 🔲部分)
                                        │        └─ boss_state (SQLite WAL)
                                        │
[match_report.py 脚本] ──Firefox profile─┤   (与 Web 互斥共用 .boss_profile)
[interview/ 独立服务] ──Ollama/DeepSeek──┘
```
