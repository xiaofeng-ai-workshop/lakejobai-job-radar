# 弱关联岗位过滤规则（已落地 match_report.py）

> 基于 2026-09-10 峰爷列出的样本梳理，已于当日确认并落地到 `match_report.py`。
> 规则位置：模块级 `KW_FILTER_RULES` / `TITLE_NON_TECH_WORDS`；过滤逻辑 `_direction_filter` + `_split_jobs`；
> 软件向 `_is_tech_job`。

## 一、机制

每个目标方向配置 `require` / `exclude` / `keep` 三条正则，对**已过相关性初筛**的候选岗判定：

1. `keep` 例外优先 —— 命中即强相关（覆盖 exclude，救回被误伤的合法岗）
2. `exclude` 命中 —— 方向弱相关，剔除
3. `require` 命中 —— 强相关
4. 都不命中 —— 方向弱相关，剔除（未命中方向必要词）

之后仍跑软件向过滤（`_is_tech_job`）。报告新增「六、已剔除的方向弱相关岗位」清单（市场模式）/「五、」清单（匹配模式）。

## 二、各方向规则（终版）

| 方向 | require（标题须命中其一） | exclude（标题命中即弱相关） | keep（救回） |
|---|---|---|---|
| **AI测试** | `测试`、`QA`、`质量`、`质检` | `漫剧`、`剧本`、`真人生成`、`训练师`、`硬件`、`产品经理`、`管培生`、`信息化管理`、`销售`、`助理` | — |
| **FDE** | `(?i)FDE`、前沿部署、效能顾问/工程师、实施/交付/部署/应用工程师、智能体开发、`Agent.*开发`、`开发.*Agent`、`Agent.*工程师` | `助理`、`管培生`、`算法工程师`、`软件测试`、`路由器/WiFi测试`、`销售顾问`、`销售经理` | `Agent.*开发`、`开发.*Agent`、`Agent.*工程师`、`Agent.*评测`、`FDE.*售前` |
| **AI项目经理** | `(AI/人工智能/智能/大模型/Agent/AIGC/ai)` 与 `(项目经理/项目管理/PM/项目主管/项目助理)` 邻接(容差8字) | `产品经理`、`售前`、`总经理`、`BG总经理`、`事业部总经理`、`内容生态`、`远程交付`、`销售经理`、`供应链经理`、`硬件`、`成本产品`、`脚本开发` | `车载AI产品经理`、`AI交付领域经理`、`AI研发技术经理`、`AI.{0,8}(技术经理/研发经理/研发.*经理/交付领域经理)`、`车载.{0,4}AI.{0,4}产品经理` |
| **项目经理** | `项目经理`、`项目管理`、`PM`、`项目主管`、`项目助理` | `AI/人工智能/AIGC/大模型/Agent`、`产品经理`、`售前`、`总经理`、`BG总经理`、`事业部总经理`、`弱电`、`门店`、`酒店`、`物业`、`风电`、`光伏`、`冶金`、`人资`、`施工`、`电力工程`、`防腐`、`保温`、`业务架构师`、`成本产品`、`团餐`、`铁路`、`路桥`、`建筑`、`市政`、`门窗`、`锂电池`、`机加工`、`汽车零部件`、`远程交付` | — |

## 三、软件向过滤增强

- `TECH_WORDS` 增补：`测试`、`QA`、`质量`（避免 AI测试 岗被软件向误伤）
- 非软件行业词（`TITLE_NON_TECH_WORDS`）**只在标题 / 公司名生效**，JD 文本只判软件正向词，避免"服务建筑行业"的信息化 PM 误伤。

## 四、峰爷确认的边界决策（2026-09-10）

1. **FDE**：`Agent后端开发工程师`、`AI agent开发`、`Agent评测工程师`、`FDE售前工程师` —— **全部保留**（靠 require/keep 命中）。
2. **AI项目经理**：`电梯安装售后项目经理` —— **剔除**；`车载AI产品经理`、`AI交付领域经理`、`AI研发技术经理` —— **保留**（进 keep 例外）。
3. **项目经理**：`项目经理（远程交付）` —— **剔除**（补进 exclude）；`讯飞医疗-医疗信息化项目经理` 等医疗信息化 PM —— **保留**。
   - 注：`三终端医药经理(AI医药医疗项目)`、`湖北省KA经理(AI医药医疗项目)` 因 title 含"AI"，在项目经理方向被 exclude AI 剔除（它们不在 AI项目经理 报告 scope 内），未作为强相关出现。如需在任一报告保留，告知后调规则。
4. **通用**：接受"非软件行业词只在标题/公司名生效"；接受 `测试/QA/质量` 进 `TECH_WORDS`。

## 五、落地效果（2026-09-10 库内数据）

| 方向 | 强相关 | 方向不符剔除 | 非软件剔除 | 噪音 | 弱相关(词根) |
|---|---|---|---|---|---|
| AI测试 | 19 | 10 | 0 | 0 | 0 |
| FDE | 41 | 9 | 1 | 6 | 0 |
| AI项目经理 | 19 | 30 | 0 | 2 | 0 |
| 项目经理 | 46 | 68 | 5 | 2 | 0 |

报告已生成：`reports/2026年09月10日-{AI测试|FDE|AI项目经理|项目经理}（武汉）·软件向.md`（覆盖写）。
